%% Rays class

classdef c_Rays
	properties
        face_source  % index line source faces 
        p_source     % coordinates source points
		dir          % direzione del raggio (always normalized)   

        face_from    % faccai da cui proviene il raggio
        p_from       % coordinates provenienza points
        dir_from     % direzione di provenienza raggio

    end

    methods

    end
end
